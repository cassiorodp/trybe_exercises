const upperCase = (string) => string.toUpperCase();

const onlyFirstLetter = (string) => string[0];

const joinStrings = (stringA, stringB) => stringA + stringB;

module.exports = { upperCase, onlyFirstLetter, joinStrings }